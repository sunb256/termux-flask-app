[project]
name = "termux-flask-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sunb256", email = "tasato56@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
app = "app:main"

[tool.uv.build-backend]
module-name = "app"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
